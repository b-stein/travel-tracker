import User from './user';

class Agent extends User {
	constructor(username, pwd) {
		super(username, pwd);
		this.id = 1;
	}

	//off trips data, return all trips "pending"

	//off trips data, find revenue gained this yr

	//off today's date + trips data, find all users on a trip today	
}

export default Agent;